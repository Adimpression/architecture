Queue:

Cache:

In Memory:

Local:

Local / Identified:
  Out:
    - Reference String

Remote:

Remote / Identified:
  Out:
    - Ip String

Queue / Provisioned:
  Out:
    - Queue

Queue / Local:
  Out:
    - Local / Identified
Queue / Remote:
    - Remote / Identified

Google Cloud Platform:

Google Cloud Platform / Pub Sub:

Google Cloud Platform / Pub Sub / Provisioned:
  Out:
    - Queue / Remote

Amazon Web Services:

Amazon Web Services / Sqs:

Amazon Web Services / Sqs / Provisioned:
  Out:
    - Queue / Remote


Micro Services:
  Out:
    - Micro Service / Identified

Micro Service:

Micro Service / Identified:
  Out:
    - Id
    
Micro Service / Communication:
  Out:
    - Shared Communication Interface String
    - Shared Protocol String

Compute:

Compute / Bare Metal:

Compute / Docker:

Compute / Virtual Machine:

Docker:

Bare Metal:

Virtual Machine:

Id:
  Out:
    - Value String

Solution Considerations:
  Out:
    - Local Memory
    - Remote Memory
    - Local Queue
    - Remote Queue
    - Local Compute Boolean
    - Remote Compute Boolean
    - Local Cache Boolean
    - Remote Cache Boolean
    - Local Storage Boolean
    - Remote Storage Boolean
    - Rdbms Boolean
    - Nosql Boolean
    - Object Storage Boolean
    - Distributed File System Boolean
    - Three Tier Boolean
    - Cqrs Boolean
    - Micro Services Boolean
    - Bpmn Boolean
    - Rules Engine Boolean
    - Pure Rest Api Boolean
    - Rpc Boolean
    - Graphql Boolean
    - Local Singleton Boolean
    - Remote Singleton Boolean
    - Local Sesson Boolean
    - Remote Sesson Boolean
    - Local Controller Boolean
    - Remote Controller Boolean
    - Stream Processing Boolean
    - Batch Processing Boolean
    - Stream And Batch Processing Boolean
    - Rdbms Transactional Boolean
    - Distributed Transactional Boolean
    - Idempotent Boolean
    - Immutable Boolean
    - Versioned Boolean
    - Playback Boolean
    - Local Runtime Logic Boolean
    - Remote Runtime Logic Boolean
    - Programming Language Dependent Serialization Boolean
    - Tool Dependent Identifiers Boolean
    - Columnar Crud Boolean
    - Row Crud Boolean
    - Immutable Data Join Boolean
    - Mutable Data Join Boolean
    - Ordered Data Boolea n
    - Distributed Data Ordered Boolean
    - Stored Ordered Data Boolean
    - Stored Bucketed Data
    - Additive Data Boolean
    - Set Data Boolean
    - Distributed Set Data Boolean
    - Local Set Data Boolean
    - Remote Set Data Boolean
    - Redundacy Boolean
    - Immutable Services Boolean
    - Point In Time Recovery Boolean